<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:padding="@dimen/activity_vertical_margin"
    android:layout_height="match_parent">
    <ImageView
        android:layout_marginTop="@dimen/marginTwenty"
        android:layout_width="match_parent"
        android:layout_height="@dimen/logoSize"
        android:layout_gravity="center"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        android:src="@drawable/logo"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/marketPlace"
        android:textSize="@dimen/textSizeMarketPlace"
        android:textColor="@color/marketTextColor"
        android:layout_gravity="center"/>

    <Button
        android:id="@+id/btnSignUpLaunch"
        android:layout_width="match_parent"
        android:layout_height="@dimen/loginLogoutButtonSize"
        android:layout_marginTop="@dimen/marginEighty"
        android:text="@string/signUp"
        android:textColor="@android:color/white"
        android:background="@drawable/rounded_button"
        />

    <Button
        android:id="@+id/btnLoginLaunch"
        android:layout_width="match_parent"
        android:layout_height="@dimen/loginLogoutButtonSize"
        android:layout_marginTop="@dimen/marginTwenty"
        android:text="@string/login"
        android:textColor="@android:color/white"
        android:background="@color/loginButtonColor"
       />
</LinearLayout>